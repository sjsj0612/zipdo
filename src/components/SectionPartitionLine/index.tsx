import {View} from 'react-native';
import React from 'react';

import styles from './style';

const SectionPartitionLine = () => {
	return <View style={styles.line} />;
};

export default SectionPartitionLine;
