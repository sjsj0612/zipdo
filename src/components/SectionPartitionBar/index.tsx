import React from 'react';
import {View} from 'react-native';

import styles from './style';

const SectionPartitionBar = () => {
	return <View style={styles.bar} />;
};

export default SectionPartitionBar;
